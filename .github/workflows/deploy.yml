name: 🚀 Deploy Polaris API

on:
  workflow_run:
    workflows: ["🧪 Test Polaris API"]
    types:
      - completed
    branches: [main]

jobs:
  deploy:
    name: 🚀 Deploy to Production
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    # TODO: Adicionar steps reais de deploy quando a infra estiver pronta.
    # Exemplos: SSH no servidor, docker build+push, ou sync de arquivos.
    #
    # - name: 🔑 Setup SSH
    #   uses: webfactory/ssh-agent@v0.9.0
    #   with:
    #     ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}
    #
    # - name: 🚀 Deploy via SSH
    #   run: |
    #     ssh ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} << 'EOF'
    #       cd /polaris-prod/polaris-v2-api
    #       git pull origin main
    #       make install-cpu
    #       make restart-all
    #     EOF

    - name: 🔔 Deployment summary
      run: |
        echo "🎉 Pipeline completed successfully!"
        echo "📅 Timestamp: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "🔗 Commit: ${{ github.sha }}"
        echo "👤 Author: ${{ github.actor }}"
        echo "⚠️  Deploy automático ainda não configurado — executar manualmente no servidor."
