# Polaris Integrations requirements.txt
# Obs: pacotes compartilhados com polaris_api (fastapi, uvicorn, torch,
#      transformers, huggingface-hub, groq, python-dotenv, prometheus-client,
#      requests) já são instalados via polaris_api/requirements.txt.
#      Aqui ficam apenas as dependências exclusivas do integrations.

TTS==0.22.0
faster-whisper==1.2.1
python-telegram-bot==22.6
httpx>=0.26.0,<1.0.0
moviepy>=1.0.3,<2.0.0
